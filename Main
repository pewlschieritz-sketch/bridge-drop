import React from 'react'
import ReactDOM from 'react-dom/client'
import BridgeDrop from './BridgeDrop.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BridgeDrop />
  </React.StrictMode>
)
